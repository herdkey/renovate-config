{
  packageRules: [
    {
      description: "Group Terraform core + providers; split majors from minors/patches",
      matchManagers: ["terraform", "terraform-version"],
      matchDatasources: [
        "terraform-provider",
        "terraform-module",
        "terraform-version",
      ],
      groupName: "terraform",
      separateMajorMinor: true,
    },
  ],
}
